import React from "react";
import Stepper from "./Stepper";

const SetupStepPageComponent = ({ activeStep }) => (
  <>
    <Stepper activeStep={activeStep} />
  </>
);

export default SetupStepPageComponent;
